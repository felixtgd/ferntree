// Entry point — bootstraps router
import './styles/global.css';

// TODO: Phase 3 — render shell, register router, initialise sidenav
document.getElementById('app')!.innerHTML = '<p style="padding:2rem">Ferntree — scaffolding complete</p>';
