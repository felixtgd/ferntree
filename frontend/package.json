{
  "name": "ferntree",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.node.json && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "chart.js": "^4",
    "zod": "^3"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5",
    "vite": "^5"
  }
}
